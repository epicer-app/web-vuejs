import Vue from 'vue';
import Vuex from 'vuex';

import { recipeList } from './recipeList';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    recipeList,
  },
});
